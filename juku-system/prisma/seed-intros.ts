// 既存 Article を全削除して、新しい 5 件のイントロダクションを投入する。
//
// Run modes:
//   npx tsx prisma/seed-intros.ts --dry-run
//   npx tsx prisma/seed-intros.ts --commit
//
// On --commit: writes prisma/.intros-replaced-<ISO>.json (削除/作成 ID のログ)

import { PrismaClient } from "../src/generated/prisma/client";
import { PrismaNeon } from "@prisma/adapter-neon";
import { writeFileSync } from "node:fs";
import { resolve } from "node:path";
import "dotenv/config";

const args = process.argv.slice(2);
const dryRun = args.includes("--dry-run");
const commit = args.includes("--commit");

if (!dryRun && !commit) {
  console.error("Pass either --dry-run or --commit");
  process.exit(1);
}
if (dryRun && commit) {
  console.error("Pass only one of --dry-run or --commit");
  process.exit(1);
}
if (!process.env.DATABASE_URL) {
  console.error("DATABASE_URL is not set");
  process.exit(1);
}

const adapter = new PrismaNeon({ connectionString: process.env.DATABASE_URL });
const prisma = new PrismaClient({ adapter });

const TODAY = new Date("2026-05-09T00:00:00.000Z");

const intros: { title: string; audience: string; category: string; body: string }[] = [
  {
    title: "【導入ガイド】講師の方へ",
    audience: "teacher",
    category: "システム使い方",
    body: `# 【導入ガイド】講師の方へ

このページでは、講師の方がシステムを使い始めるまでにやっていただくことをまとめています。上から順番に進めてください。

---

## 1. サインアップ

導入期間中は、運営から共有されたサインアップ URL から自分でアカウントを作成してください。

- URL: \`/signup/teacher\`
- 必要項目: 氏名、メール、電話番号、雇用形態、担当可能科目、大学情報など
- 初期パスワードは一律 \`password123\` です
- 登録完了後、\`/login\` から上記メール + \`password123\` でログイン
- ログイン後、必ず **サイドバー下部の「パスワード変更」** から自分のパスワードに変更してください

> 招待制に戻った後は、運営から登録完了のお知らせを受けてからログインします。

---

## 2. 自分の生徒を招待

担当する生徒に、運営から共有されたサインアップ URL を案内してください。

- URL: \`/signup/student\`
- 生徒側の入力: 氏名、メール、保護者情報、卒業年度、高校名など
- 初期パスワードは一律 \`password123\`
- 登録後、生徒は \`/login\` からログインできます

---

## 3. 担当生徒との紐付け

運営が「講師詳細 → 担当生徒」で紐付けを行います。あなたの担当が確定すると、ダッシュボードや進捗・面談画面で生徒情報を扱えるようになります。

---

## 4. 招待した生徒の大目標と週次目標を登録

生徒の学習計画を立てるため、まず大目標と週次目標を登録します。

### 大目標

サイドバー「生徒管理」 → 該当生徒 → ページ下部の「大目標進捗」セクションの「大目標を追加」ボタン。

- 科目、教材名、目標ページ数、開始日（初期値: 今日）、完了期限を入力
- 例: 「数学2BC / 青チャート数学IIB / 480 ページ / 5月9日 〜 9月9日」

### 週次目標

「週次目標進捗」セクションの「週次目標を追加」。大目標を選んでぶら下げる必要があります。

- 紐づく大目標を選択 → 科目・教材名は自動入力
- 目標ページ数、週の開始日（初期値: 今日）、週の終了日（自動で +7 日）

> 生徒自身も \`/my-goals\` から大目標・週次目標を作成できますが、自分が作成したもののみ編集・削除できます。

---

## 5. ルーティンタスクの登録

毎回の出勤打刻時に自動でその日のタスクとして展開される定期作業を登録します。

サイドバー「タスク管理」 → 上部の「ルーティン管理」ボタン。

- タスク名（例: 教室清掃）、種別、対象生徒（任意）、説明
- 出勤打刻すると、当日 23:59 期限のタスクとして自動生成

---

## 6. 学校イベントの登録

担当生徒の高校の試験日や行事を登録しておくと、ダッシュボードのカレンダーに表示されます。

サイドバー「イベント管理」 → 「イベントを追加」。

- 学校が未登録の場合はまず学校を追加
- イベント種別: 試験 / 文化祭 / 体育祭 / 修学旅行 / その他

---

## 導入後

- 普段の運用については「【日常運用】講師の方へ」を参照
- 機能の細かい仕様（演算方法など）は「【機能リファレンス】講師の方へ」を参照`,
  },
  {
    title: "【日常運用】講師の方へ",
    audience: "teacher",
    category: "システム使い方",
    body: `# 【日常運用】講師の方へ

オペレーションに沿って、毎日の業務でやることをまとめています。

---

## 1. 出勤時にやること

### システム起動
1. 校舎のタブレット / PC で本システムを開く
2. 自分のメール + パスワードでログイン

### 出勤打刻
1. ダッシュボード上部の「出退勤」ボタン
2. 「出勤」を押す → 自動的に当日のシフトと突合
3. 設定したルーティンタスクが当日 23:59 期限のタスクとして自動生成されます

### タスク対応
- ダッシュボードの「未完了タスク」カードに今日処理すべきタスク一覧
- 完了したものは左のチェックボックスで完了マーク
- タスク名をクリックすると編集ページ
- 新しいタスクは「タスク管理」ページの上部からタイトルだけで素早く追加可能

---

## 2. アラートがある生徒の対応

ダッシュボード「未読アラート」カードに最大 20 件まで表示されます。

- 種類: 期限超過タスク / 面談リマインド / 進捗ペース警告 / その他
- 内容を確認し、必要なら生徒詳細から進捗登録・面談記録などをフォロー
- 「既読」ボタンで処理済み扱い → 次のアラートに集中できます

サイドバー「生徒管理」の生徒名横に **赤いアラートバッジ** が出ている生徒は、未対応のアラートが残っています。

---

## 3. 面談の対応

サイドバー「面談管理」で、最近の面談記録一覧 + 検索（生徒名・面談タイプ）が見られます。

### 面談記録の追加
1. 生徒詳細ページ → 「面談記録」セクションの「面談記録を追加」
2. 面談日、時間、タイプ（定期面談 / 進路相談 / 学習相談 / 保護者面談 / その他）、ステータス、内容を記入
3. 親御さんへの共有コメント欄を埋めると、保護者向け報告書に自動掲載されます
4. 次回面談予定を入力するとリマインドが自動生成

### 面談記録の編集
- 「面談管理」ページのカード右上「編集」ボタンから、その場で開いて編集可能
- 削除も同じ場所から

---

## 4. 自習室対応

サイドバー「自習室管理」。

### 入退室の確認
- 校舎ごとのカードに、現在入室中の生徒一覧と席種別ごとの空き状況
- 生徒は校舎入口の QR から自分でチェックイン

### 席移動の案内
- 例: 授業でテーブル席を使う必要がある場合、テーブル席で自習中の生徒にカウンター席への移動を依頼
- 入室・席種は自動で保存されているので、生徒が再度チェックインすれば移動も反映されます

### Pt 交換
- 生徒は自習室を 1 日 1 回利用すると 1pt 獲得
- 一定 pt 貯まると景品交換可能（運営運用ルールに従ってください）
- 履歴は「自習室管理」のページ下部から確認

### 席数操作
- 校舎カード内の「ブース席 / テーブル席」の数値はクリックで編集可能
- 当日の机配置に合わせて変更してください

---

## 5. 出勤中の確認推奨ポイント

- ダッシュボードの「**本日使用するゼミプリント**」カードで、その日に必要な印刷物を事前準備
- ダッシュボードの「自習室空席状況」で各校舎の状況を把握
- 「未完了タスク」と「未読アラート」をルーチンで確認

---

## 6. 退勤時にやること

- 「出退勤」ボタンから退勤打刻
- ルーティンを含む当日タスクは、未完了でも翌日に持ち越し（自動 overdue 化されます）`,
  },
  {
    title: "【導入ガイド】生徒の方へ",
    audience: "student",
    category: "システム使い方",
    body: `# 【導入ガイド】生徒の方へ

入塾時にやっていただくことをまとめています。順番に進めてください。

---

## 1. サインアップ または 招待を受けた後の対応

### サインアップから自分で登録する場合
1. 運営から共有された URL（\`/signup/student\`）を開く
2. 氏名、メール、保護者連絡先、高校名、卒業年度などを入力
3. 登録ボタンを押すと、初期パスワード \`password123\` が表示されます
4. 自動的にログイン画面に遷移します

### 講師・運営から招待された場合
- 登録完了の連絡を受けてから \`/login\` を開く
- 入力情報: メール（招待時に伝えられたもの）+ パスワード \`password123\`

### 初回ログイン後 (必須)
- サイドバー下部の **「パスワード変更」** から、自分専用のパスワードに変更してください
- これを忘れると、別の人が \`password123\` でログインできてしまうので必ず実施

---

## 2. 大目標・週次目標の設定

サイドバー「目標管理」（\`/my-goals\`）から登録します。

### 大目標
入試までの大きな目標（例: 半年で青チャート1冊終える）を登録します。

1. 「＋ 大目標を追加」
2. 科目、教材名、目標ページ数、開始日（初期値: 今日）、期日を入力
3. 「作成」

### 週次目標
大目標を週単位に分割します。

1. 大目標カードの「＋ 追加」
2. 教材名は大目標から自動入力されます
3. ページ数、開始日（初期値: 今日）、期日（初期値: +7 日、開始日変更で連動）

> 講師・運営が登録した大目標・週次目標は閲覧のみ可能（編集・削除は不可）。自分で作ったものは「講師・運営作成」のバッジが付かず、編集・削除できます。

---

## 3. ゼミの予定日登録

サイドバー「ゼミ管理」から、自分の受験科目に対応する単元のプリント予定日を登録します。

1. マトリクス表の **空欄セル** をクリック
2. モーダルが開いて「次に登録すべきプリント No」が表示されます
   - **No は連番でしか登録できません**。例: 1 〜 3 まで登録済みの状態で No.5 をクリックしても、実際には No.4 の予定が登録されます
3. 予定日を入力して「登録」
4. 黄色いセルは「予定あり」、緑のセルは「完了済み」、白のセルは「未登録」

予定日を変更したい場合は黄色いセルをクリックして編集モーダルを開きます。

> 講師・運営も同じ画面で、あなたが登録した予定を見ることができます。

---

## 4. 学習スケジュールの登録

サイドバー「学習スケジュール」（\`/study-schedule\`）から、曜日ごとに「いつ何の科目を何分やるか」を登録します。

- 棒グラフをクリックしてスロット（科目 + 分数）を編集
- 1 日最大 24 時間まで
- このスケジュールは、ダッシュボードの「**今日進めるページ**」の計算に使われます

---

## 導入後

- 機能の細かい仕様や計算方法を知りたい場合は「【機能リファレンス】生徒の方へ」を参照してください`,
  },
  {
    title: "【機能リファレンス】講師の方へ",
    audience: "teacher",
    category: "システム使い方",
    body: `# 【機能リファレンス】講師の方へ

各機能の使い方と内部仕様（特に集計・演算ロジック）をまとめています。

---

## 大目標と週次目標

### 構造
- **大目標 (BigGoal)**: 教材1冊・科目1つに対する長期目標。期間と総ページ数を設定
- **週次目標 (LearningGoal)**: 大目標にぶら下げる週単位のサブ目標
- 進捗 (ProgressRecord) は、**大目標に直接** または **週次目標経由** のどちらにも紐づけ可能

### 進捗集計の仕様
- 週次目標の進捗 = その週次目標に \`goalId\` で紐づく ProgressRecord の \`pagesCompleted\` 合計
- **大目標の進捗 = 直接紐づく ProgressRecord の合計 + 配下の各週次目標の進捗合計**
- 紐づけのない進捗（goalId / bigGoalId 両方 null）は集計に含まれません

### 達成判定
- 進捗を登録した時点で、その goal の合計が targetPages 以上 → \`status: completed\` に自動更新
- 大目標は「直接紐づき + 配下週次合計」で判定

---

## ダッシュボードの「今日進めるページ」（生徒側）

\`todayPlan.ts\` の \`computeTodayPlan\` で計算されています。週次目標ごとに以下を実行:

1. 期日切れ・完了済みの目標は除外
2. 残ページ数 = \`targetPages - done\`
3. 今日のスケジュールから「この科目の今日の分数」を取得
4. 今日〜期日までの「この科目の総分数」を集計
5. **todayPages = ⌈ 残ページ × (今日の分数 / 残り日数の総分数) ⌉**
6. 例外:
   - 残り日数のスケジュールにこの科目なし → 「残り日数のスケジュールにこの科目がありません」
   - 今日のスケジュールにこの科目なし → 「本日のスケジュールにこの科目がありません」

学習スケジュールが未設定なら、生徒には「設定を促すリンク」が表示されます。

---

## 大目標カード（生徒詳細）の各列の意味

\`GoalsPanel.tsx\` の横断カレンダー表で、大目標ごとに 3 行表示されます。

- **正常予定**: 開始日から期日まで均等に進めた場合の累計（参考線）
- **実績 / 調整**: 過去週は実績累計のみ。今週は「実績 / 演算結果（残ページを今日〜期日で再配分した予測）」を表示。未来週は調整予測のみ
- **現ペース**: 開始日から最新進捗日までの実ペースを延伸した予測。targetPages に達した週で「達成」と表示

「N 日遅れ」: 正常予定との差を、現状の日割りペースで日数換算した値。

---

## ゼミプリント（連番運用）

- 単元ごとに \`printCount\` 枚ある中で、生徒は **最小空き No** から順に予定登録
- マトリクス空欄セルクリック → 次の登録可能 No の予定登録モーダル
- 既存の番号を上書き登録すると予定日変更扱い（連番チェックはスキップ）
- 完了済みセルは編集不可、予定済みセルはクリックで編集可

---

## ルーティンタスク

- 講師ごとに「タスク名 / 種別 / 対象生徒（任意）」を登録
- 出勤打刻 (\`/api/attendance/clock-in\`) すると、ルーティンが当日 23:59 期限のタスクとして展開される
- 講師は \`/tasks/routines\` で自分のルーティンのみ管理可能

---

## アラート

\`/api/alerts/check-all\` で集計され、\`alert\` テーブルに作成されます。

- **task_overdue**: タスク期限切れ
- **shift_reminder**: シフト関連リマインド
- **progress_warning**: 学習進捗のペース遅延
- **general**: その他（運営が手動で出すお知らせ等）

ダッシュボードには未読のみ最大 20 件表示。

---

## 自習室

- セッション (\`StudyRoomSession\`) は \`checkInAt\` のみで保存され、\`checkOutAt\` が null のものが「現在入室中」
- 校舎の \`closeTime\` を超えて未退室のものは自動退室扱い (\`autoCheckedOut: true\`)
- 1 日 1 セッション完了で \`pointAwarded: true\` + \`PointTransaction\` に +1 pt が記録
- 校舎ごとの席容量は \`StudyRoomConfig.boothCapacity / tableCapacity\` で管理

---

## 面談スナップショット

面談記録を保存すると、その時点の **大目標一覧 + 直近の進捗一覧** が JSON で凍結されます (\`goalsSnapshot\` / \`progressSnapshot\`)。後から「あの時はどう思ったか」を見返せます。

---

## バックアップ

- 毎日 03:00 JST に GitHub Actions が \`pg_dump\` を実行 → Vercel Blob (private) に保存、30 日保持
- 運営は \`/backup\` ページから手動 JSON エクスポートも可能（admin のみ）`,
  },
  {
    title: "【機能リファレンス】生徒の方へ",
    audience: "student",
    category: "システム使い方",
    body: `# 【機能リファレンス】生徒の方へ

自分が日常使う各機能の動きと、計算がどう行われているかをまとめています。

---

## 大目標 / 週次目標

### 役割の違い
- **大目標**: 教材1冊や入試までの総量を捉える長期目標
- **週次目標**: 大目標を週単位に分けたサブ目標

### 進捗の集計
- 週次目標の進捗 = その目標に紐付けて登録された学習進捗の合計ページ
- **大目標の進捗 = 大目標に直接紐付いた進捗 + 配下の週次目標の進捗合計**
- 例: 大目標「青チャート数学IIB / 480p」配下に週次目標で 30p 進めた + 大目標に直接 10p 進めた → 大目標の done は 40p

### 進捗の登録方法
\`/progress/new\` または、ダッシュボードの週次目標カードの「進捗を登録」リンクから:

1. 紐づく目標を選択（大目標または週次目標）
2. 科目・教材名は自動入力されます（変更も可）
3. ページ数と内容を入力 → 保存

紐づけない学習進捗（自由学習）も登録可能ですが、その場合は目標の進捗には加算されません。

---

## 「今日進めるページ」の計算

毎朝、ダッシュボードの「📚 今日進めるページ」カードに、週次目標ごとに「今日進めると良いページ数」が表示されます。

### 計算式
\`\`\`
今日進めるページ = ⌈ 残りページ × (今日のこの科目の学習分数 / 残り日数のこの科目の総分数) ⌉
\`\`\`

### 必要な前提
- 学習スケジュール（曜日 × 科目 × 分数）が **設定済み** であること
- 設定がなければ、「設定する →」のリンクが代わりに表示されます

### 例外メッセージ
- 「残り日数のスケジュールにこの科目がありません」: 期日まで該当科目のスケジュールが 0 分
- 「本日のスケジュールにこの科目がありません」: 残り日数には他日にあるが、今日のスケジュールにはない

---

## 大目標カードのペース

ダッシュボードの「🎯 大目標」カード:

- 進捗バーは「done / targetPages」の割合
- 期日と現在の進捗から、十分なペースで進めているかを内部判定（遅れている場合はカード色が変わるなど）

---

## ゼミプリント

サイドバー「ゼミ管理」のマトリクス表:

- **白セル**: 未登録 → クリックで予定登録モーダル
- **黄セル**: 予定あり → クリックで編集（予定日変更・完了マーク・削除）
- **緑セル**: 完了 → クリック不可

### 連番ルール
- プリントは必ず **連番** で登録されます
- たとえば No.1 〜 No.3 まで予定済みのとき、No.5 のセルをクリックしても、実際には **No.4** の予定が登録されます
- 同じく、No.2 を削除すると次に登録できるのは No.4 ではなく **No.2**（最小の空き番号）

---

## 学習スケジュール

\`/study-schedule\` ページ。

- 曜日ごとに「いつ何の科目を何分」を棒グラフで設定
- スロット = 1 つの「科目 + 分数」のブロック
- 1 日最大 24 時間
- ここで設定した内容が、上記「今日進めるページ」の計算に使われます

---

## ポイント (Pt)

サイドバー「ダッシュボード」の獲得ポイントカードに表示されます。

- 自習室を 1 日 1 回利用すると **+1 pt**（同日に複数回入退室しても 1 pt）
- その他、講師・運営が任意に付与する場合あり (\`PointTransaction\`)
- 一定 pt で景品と交換可能（運用ルールは校舎ごとに案内）

---

## 自習室

サイドバー「自習室管理」（admin/teacher のみ）。生徒は校舎入口の QR コードからチェックインします。

### チェックイン
- 校舎の QR を読み取る → 自動で校舎が選ばれた状態で入室画面が開く
- 席種（ブース / テーブル）を選んで「入室」

### 退室
- システム上で「退室」を押す → \`checkOutAt\` が記録され pt が確定
- \`closeTime\` を超えて未退室の場合は自動退室扱いになりますが、できるだけ自分で退室してください

---

## アラート

ダッシュボード「未読アラート」カードに、自分宛のアラートが最大 20 件表示されます。

- 期限切れタスク、進捗の遅れ、運営からのお知らせ等
- 「既読」ボタンで一覧から消えます

---

## パスワード

- 初期パスワードは \`password123\`
- ログイン後、サイドバー下部の「パスワード変更」から必ず変更してください
- 忘れた場合は運営にお問い合わせください（運営が \`password123\` にリセットしてくれます）`,
  },
];

async function run() {
  const ROLLBACK = Symbol("dry-run-rollback");
  const stats = {
    deletedReads: 0,
    deletedArticles: 0,
    createdArticles: [] as { id: string; title: string }[],
  };

  try {
    await prisma.$transaction(async (tx) => {
      // 1) 既存 Article をすべて削除（ArticleRead は ON DELETE CASCADE で連動）
      const r1 = await tx.articleRead.deleteMany({});
      stats.deletedReads = r1.count;
      const r2 = await tx.article.deleteMany({});
      stats.deletedArticles = r2.count;

      // 2) 新しい 5 件を投入
      for (const a of intros) {
        const created = await tx.article.create({
          data: {
            title: a.title,
            body: a.body,
            audience: a.audience,
            category: a.category,
            images: "[]",
            publishedAt: TODAY,
          },
          select: { id: true, title: true },
        });
        stats.createdArticles.push(created);
      }

      if (dryRun) throw ROLLBACK;
    }, { timeout: 60000 });
  } catch (e) {
    if (e === ROLLBACK) {
      console.log("=== DRY RUN COMPLETE (rolled back) ===");
      console.log(JSON.stringify({
        wouldDelete: { articleReads: stats.deletedReads, articles: stats.deletedArticles },
        wouldCreate: stats.createdArticles.map((a) => a.title),
      }, null, 2));
      return;
    }
    throw e;
  }

  const ts = new Date().toISOString().replace(/[:.]/g, "-");
  const path = resolve(process.cwd(), `prisma/.intros-replaced-${ts}.json`);
  writeFileSync(path, JSON.stringify({ replacedAt: new Date().toISOString(), ...stats }, null, 2));
  console.log("=== COMMIT COMPLETE ===");
  console.log(`Log: ${path}`);
  console.log(JSON.stringify({
    deleted: { articleReads: stats.deletedReads, articles: stats.deletedArticles },
    created: stats.createdArticles.map((a) => a.title),
  }, null, 2));
}

run()
  .then(() => prisma.$disconnect())
  .catch(async (e) => {
    console.error(e);
    await prisma.$disconnect();
    process.exit(1);
  });
